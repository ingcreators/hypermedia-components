{
  "$description": "Emerald color theme. Emitted under [data-color=\"emerald\"]. Every axis is the same shape: `600` for the resting action surface, `700` for hover, `500` for the focus ring, and white text. That is not a coincidence per hue — the ladder puts `600` at L 0.54, and white clears 4.5:1 below L 0.554 on every hue, so the foreground follows from the lightness rather than from a per-theme judgement.",
  "color": {
    "focus-ring": { "$type": "color", "$value": "{primitive.color.green.500}" },
    "action": {
      "primary": {
        "bg":     { "$type": "color", "$value": "{primitive.color.green.600}" },
        "fg":     { "$type": "color", "$value": "{primitive.color.white}" },
        "border": { "$type": "color", "$value": "{primitive.color.green.600}" }
      },
      "primary-hover": {
        "bg":     { "$type": "color", "$value": "{primitive.color.green.700}" },
        "border": { "$type": "color", "$value": "{primitive.color.green.700}" }
      },
      "primary-soft": {
        "bg": { "$type": "color", "$value": "color-mix(in oklab, {primitive.color.green.600} 12%, transparent)" }
      }
    }
  }
}

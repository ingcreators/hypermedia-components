---
title: Confirm action
description: Show a confirmation dialog before sending a destructive htmx request.
---

import Demo from '../../../components/Demo.astro';
import ConfirmActionDemo from '../../../components/recipe-demos/ConfirmActionDemo.astro';

`confirm-action` adds a modal confirmation step in front of a
destructive action — an htmx request or a plain form submission. The
button stays a normal `<button>`; htmx remains the sole owner of the
network request; a small behavior shim opens the dialog and re-emits a
`hc:confirmed` event when the user accepts. For a submit button in a
plain (non-htmx) form, the behavior also completes the intercepted
submission via `form.requestSubmit()`.

:::note[Setup]
This recipe needs the `installConfirm()` behavior — without it the button
fires the request immediately, with no confirmation step. Install every
behavior at once with `import '@hypermedia-components/core/behaviors';`, or
just this one:

```js
import { installConfirm } from '@hypermedia-components/core';
installConfirm();
```

See [Installation](/hypermedia-components/start/installation/).
:::

:::tip[Scaffold]
`npx @hypermedia-components/cli add confirm-action` copies this recipe's scaffold
(`recipe.html`, `expanded.html`, `contract.md`, `checks.json`);
`npx @hypermedia-components/cli validate` machine-checks your copy. The
full contract lives in
[`recipes/confirm-action/contract.md`](https://github.com/ingcreators/hypermedia-components/blob/main/recipes/confirm-action/contract.md).
:::

*Also known as: confirmation dialog, are-you-sure prompt.*

## Live demo

Click **Delete** and confirm — the row disappears via the `outerHTML`
swap and a real `HX-Trigger` header raises a toast naming the deleted
item. Unlike the no-server preview under *Basic usage* below (which only
reports the outcome), this table round-trips through a real endpoint: a
namespaced demo implementation of the server contract under
`api/recipes/confirm-action/`. The server is stateless, so **Reset
rows** simply refetches the rows.

<ConfirmActionDemo />

## Basic usage

Add `data-hc-confirm` to any element that issues an htmx request, and
gate the htmx trigger on `hc:confirmed`:

```html
<button
  class="hc-button"
  data-variant="error"
  type="button"
  data-hc-confirm="Delete this item?"
  data-hx-delete="/items/123"
  data-hx-trigger="hc:confirmed"
  data-hx-target="closest tr"
  data-hx-swap="outerHTML">
  Delete
</button>
```

Try it — clicking **Delete** opens the shared confirm dialog. (This demo
has no server, so confirming just reports the outcome instead of firing an
htmx request.)

<Demo previewClass="hc-preview--start">
  <button class="hc-button" data-variant="error" id="confirm-demo-btn"
    data-hc-confirm="Delete this item? This cannot be undone."
    data-hc-confirm-title="Delete item"
    data-hc-confirm-label="Delete"
    data-hc-cancel-label="Keep">
    Delete
  </button>
  <output id="confirm-demo-out" aria-live="polite" style="margin-inline-start:.75rem;color:var(--hc-color-text-muted);"></output>

  <Fragment slot="code">

  ```html
  <button
    class="hc-button"
    data-variant="error"
    type="button"
    data-hc-confirm="Delete this item?"
    data-hx-delete="/items/123"
    data-hx-trigger="hc:confirmed"
    data-hx-target="closest tr"
    data-hx-swap="outerHTML">
    Delete
  </button>
  ```

  </Fragment>
</Demo>

What happens:

1. The user clicks the button.
2. The behavior intercepts the click and shows the shared confirm
   dialog. The original click is suppressed; htmx does not fire yet.
3. If the user confirms, the behavior dispatches a bubbling `hc:confirmed`
   event on the original button.
4. htmx, listening via `data-hx-trigger="hc:confirmed"`, sends the request.
5. The server returns HTML for the target area (and optionally
   `HX-Trigger` events).

If the user cancels, no event is dispatched and nothing happens.

## Plain forms (no htmx)

`data-hc-confirm` also works on a submit button in an ordinary
`<form>` — no htmx attributes required:

```html
<form method="post" action="/items/123/delete">
  <button class="hc-button" data-variant="error"
    data-hc-confirm="Delete this item?">
    Delete
  </button>
</form>
```

On confirm, the behavior calls `form.requestSubmit(button)`, so the
intercepted submission completes exactly as the click would have: the
button is the submitter (its `formaction`/`formmethod` are honored)
and constraint validation runs first. On cancel, nothing is submitted.

This path applies only when neither the button nor its form carries an
htmx verb attribute (`hx-get/post/put/patch/delete` or the `data-hx-*`
variants). As soon as either does, only `hc:confirmed` is dispatched
and htmx owns the request — the form is never double-submitted.

Without JavaScript the same markup degrades gracefully: the form
submits natively, just without the confirmation step.

## Customizing the dialog

The message comes from `data-hc-confirm`. The remaining attributes are
optional:

| Attribute                   | Default                         |
| --------------------------- | ------------------------------- |
| `data-hc-confirm`           | _(required)_ dialog body text   |
| `data-hc-confirm-title`     | `Confirm`                       |
| `data-hc-confirm-label`     | `Confirm`                       |
| `data-hc-cancel-label`      | `Cancel`                        |
| `data-hc-confirm-variant`   | source's `data-variant`, else `primary` |

```html
<button
  class="hc-button"
  data-variant="error"
  type="button"
  data-hc-confirm="Permanently delete invoice #123? This cannot be undone."
  data-hc-confirm-title="Delete invoice"
  data-hc-confirm-label="Delete"
  data-hc-cancel-label="Keep"
  data-hx-delete="/invoices/123"
  data-hx-trigger="hc:confirmed"
  data-hx-target="closest tr">
  Delete
</button>
```

## Expanded HTML

Wrapping the button in `.hc-action` with an htmx indicator gives the
full destructive-action experience from plan §24:

```html
<span class="hc-action">
  <button
    class="hc-button"
    data-variant="error"
    type="button"
    data-hc-confirm="Delete this item?"
    data-hx-delete="/items/123"
    data-hx-trigger="hc:confirmed"
    data-hx-target="closest tr"
    data-hx-swap="outerHTML"
    data-hx-disabled-elt="this"
    data-hx-indicator="closest .hc-action">
    Delete
  </button>
  <span class="hc-spinner htmx-indicator" aria-hidden="true"></span>
</span>
```

## Macro form

If you import `@hypermedia-components/core/macros`, you can write the
same pattern as a single custom element:

```html
<hc-confirm-action
  method="delete"
  action="/items/123"
  target="closest tr"
  swap="outerHTML"
  variant="error"
  message="Delete this item?"
  confirm-label="Delete"
  cancel-label="Keep">
  Delete
</hc-confirm-action>
```

The element runs once on `connectedCallback` and replaces its
children with the expanded HTML shown above, then calls
`htmx.process(this)` so htmx picks up the new attributes. The
upgrade is idempotent — re-attaching the element does not re-expand
it.

Attributes:

| Attribute        | Default                    | Maps to                          |
| ---------------- | -------------------------- | -------------------------------- |
| `action`         | _(required)_               | URL for the htmx request         |
| `method`         | `post`                     | `data-hx-{method}`               |
| `target`         | _(omitted)_                | `data-hx-target`                 |
| `swap`           | `outerHTML`                | `data-hx-swap`                   |
| `variant`        | _(omitted)_                | `data-variant` on the button     |
| `message`        | `Continue?`                | `data-hc-confirm`                |
| `title`          | _(omitted)_                | `data-hc-confirm-title`          |
| `confirm-label`  | _(omitted)_                | `data-hc-confirm-label`          |
| `cancel-label`   | _(omitted)_                | `data-hc-cancel-label`           |
| `disabled-elt`   | `this`                     | `data-hx-disabled-elt`           |
| `indicator`      | `closest .hc-action`       | `data-hx-indicator`              |
| `no-spinner`     | _(boolean)_                | Omit `.hc-spinner` if set        |

The macro is **optional** — the documented contract is the expanded
HTML. If you need to step outside the macro's attribute surface
(custom button content, an extra wrapper class, …), copy the
expanded HTML and edit it directly.

## Interaction with htmx — the specification

The contract between the confirm gate and htmx, spelled out (these are
browser-tested guarantees, not implementation details):

- The click is intercepted in the **capture phase** with
  `preventDefault` + `stopPropagation`, so htmx (and any other click
  handler on the element) never observes the original activation.
- `data-hx-trigger="hc:confirmed"` is therefore **required**: only the
  `hc:confirmed` event — dispatched on the trigger element when the
  user confirms — starts the request. An element with `data-hc-confirm`
  but a default click trigger never fires its htmx request.
- **Cancel dispatches nothing.** No request, no event, no submission.
- A submit button of a **plain form** (no htmx verb attribute on the
  button or its form) is submitted via `form.requestSubmit(button)`
  after `hc:confirmed` — see [Plain forms](#plain-forms-no-htmx). An
  htmx verb on either element suppresses this branch, so htmx-wired
  forms are never double-fired.
- Do **not** combine with htmx's own `hx-confirm` — htmx never sees
  the activation, so `hx-confirm` never runs; you'd be writing dead
  attributes. Use one mechanism per element.
- The interception is delegated at the document level, so triggers
  swapped in by htmx are gated without re-initialization.

See also [htmx integration → Confirm gating](/hypermedia-components/integrations/htmx/#confirm-gating-specification).

## Hyperscript alternative

If you prefer to keep the orchestration next to the markup, the same
flow can be expressed in [_hyperscript](https://hyperscript.org)
instead of relying on `installConfirm()`. Drop the `data-hc-confirm`
attribute, write the confirm step inline, and dispatch the same
`hc:confirmed` event that htmx already listens for:

```html
<button
  class="hc-button"
  data-variant="error"
  type="button"
  data-hx-delete="/items/123"
  data-hx-trigger="hc:confirmed"
  data-hx-target="closest tr"
  data-hx-swap="outerHTML"
  _="on click
       if confirm('Delete this item?')
         send hc:confirmed to me
       end">
  Delete
</button>
```

`confirm(...)` here is the browser's blocking primitive, which is
unstyled and not always desirable for destructive actions. To reuse
the shared `<dialog class="hc-confirm-dialog">` markup from a single
page-level element, see the
[hyperscript integration page](/hypermedia-components/integrations/hyperscript/#confirm-action).

When to prefer this form:

- The confirm step is genuinely one-off (a single delete button in
  the page chrome) and you don't want to import `installConfirm`.
- You already include `_hyperscript` for other reasons.

When to stick with vanilla:

- Several elements need the same confirm flow — one
  `installConfirm()` call covers all of them.
- You want the shared styled dialog without rendering the markup
  yourself.

## Server response contract

The server's response is whatever the htmx attributes specify — the
confirm behavior does not change the request shape.

For a row-level delete:

- **On success:** return HTML for the target area, or an empty body
  with a status that lets htmx swap nothing. The recipe above swaps
  `outerHTML` of the closest `<tr>`, so the server returns the new row
  HTML (or nothing if the row is gone).
- **For a toast notification:** include
  `HX-Trigger: {"hc:toast":{"message":"Deleted","variant":"success"}}`
  in the response headers.
- **On validation failure:** return a 4xx with HTML describing the
  failure, or use `HX-Reswap` / `HX-Retarget` to steer the swap.

## Accessibility

- The shared dialog uses the standard `<dialog>` element opened via
  `showModal()`. The browser handles focus trapping, Escape-to-close,
  and `inert` for content behind the dialog.
- The dialog declares `aria-labelledby` (title) and `aria-describedby`
  (message), so screen readers announce both on open.
- Focus moves to the **Cancel** button by default — destructive actions
  must require explicit confirmation, not a stray Enter keypress.
- On close — confirm, cancel, or <kbd>Escape</kbd> — the native
  `dialog.close()` **returns focus to the trigger** that opened it; the
  behavior adds no focus code of its own. If the confirmed request then
  removes that trigger from the DOM (deleting a table row is the
  canonical case), place the next focus target deliberately in the
  swap: target the surrounding region and put `autofocus` (or
  `tabindex="-1"` plus a server-driven focus hint) on the element
  keyboard users should land on, so they are not silently dropped at
  `<body>`.
- The original button keeps its native `<button>` semantics. Use a
  visible, descriptive label (`Delete`, `Discard changes`) rather than
  an icon alone.

## Progressive enhancement

Without the behavior loaded, the button still works:

- Without htmx, put the button inside a
  `<form method="post" action="/items/123/delete">` — the
  [plain-form path](#plain-forms-no-htmx) confirms and then submits
  it. An htmx-attributed button with no htmx loaded performs no
  action, exactly as the markup describes.
- Without `hc.behaviors.js`, the click bubbles normally. htmx sees no
  `hc:confirmed` event and does nothing. Adding a `<noscript>` notice or
  rendering a fully server-side confirmation page is the recommended
  graceful degradation.

## Related

- [Button](/hypermedia-components/components/button/) — the visual
  surface for the action.
- [request-action recipe](/hypermedia-components/recipes/request-action/) — the non-confirming version with spinner.
- `live-search` recipe — uses the same htmx trigger model.

---
title: 行から詳細へ
description: 行が指すレコードを開き、作業し、同じ一覧に戻る — 同じ条件、同じソート、同じページ、そして離れた行にカーソルが残ったまま。
---

業務の一覧で最もよく使われ、どのアプリでも作り直される操作です。
**このレコードを開き、作業し、戻り、次を開く。**作り直されたものの
多くは同じ 3 つを失います — 中クリック、Back ボタン、そして一覧での
居場所です。

:::tip[スキャフォールド]
```sh
npx @hypermedia-components/cli add row-detail
npx @hypermedia-components/cli validate page.html
```
`recipe.html`、`expanded.html`、機械検査される
[サーバー契約](https://github.com/ingcreators/hypermedia-components/blob/main/recipes/row-detail/contract.md)
が入ります。
:::

## リンクは本物のリンク、同一性の列に

```html
<tr class="hc-datagrid__row" id="row-4903" data-row-no="137">
  <td class="hc-datagrid__cell">
    <input class="hc-checkbox" type="checkbox" name="ids" value="4903"
           aria-label="Select order SO-4903">
  </td>
  <th class="hc-datagrid__cell" scope="row">
    <a href="/orders/4903" data-hc-row-link>SO-4903</a>
  </th>
```

**同一性**の列が持ちます。行が*である*ものであって、後ろに付け足した
動詞ではありません。ただのアンカーであるだけで、中クリック、⌘ クリック、
アドレスのコピー、Back、キーボード、JS 無しの経路が手に入ります。
クリックハンドラはそれらを下手に作り直すことになります。

`installRowLink()` が足すのは、アンカー自身にはできない 1 つだけ —
**行のどこでも Enter で開く**。矢印キーで辿り着いたグリッド利用者が
期待する挙動です。編集が該当する場所では編集が勝ち（グリッドが
エディタを開く前にイベントをキャンセルします）、Enter を自分で扱う
コントロールはそのまま、修飾キー付きは「別のこと」の意思表示です。

:::danger[リンクを行全体に広げないでください]
`::after` のオーバーレイはカードには正しく、ここでは誤りです。データ
グリッドはテキスト選択・範囲選択・TSV コピーを持っており、セルの上に
透明なアンカーを置くと 3 つとも壊れます。行サイズの当たり判定には
チェックボックスやインラインエディタの置き場所もありません。

幅の広い行のポインタ操作には、末尾の狭い列にシェブロンのリンクを
足してください。href は同じ、`aria-label` でレコード名を名乗ります。
:::

## 戻りこそ、みんなが落とす部分

一覧 URL は既に条件・ソート・列・ページを持っています。Back が復元
できないのは**どの行にいたか**だけなので、詳細の*一覧に戻る*はその
URL ＋ `#row-<id>` です。

```text
/orders?f-ship=@week-start..&sort=-ship,order&page=4#row-4903
```

[`installDatagrid()`](/hypermedia-components/ja/components/datagrid/#フラグメントナビゲーション)
が**アクティブセル**をそこに着地させ、グリッド自身のスクロールポートを
スクロールします。キーボード利用者も目と同じ場所に到着します。

| 状態 | 復元方法 |
| --- | --- |
| 条件・ソート・列・ページ | 一覧 URL |
| どの行か | `#row-<id>` |
| 選択のチェック | 選択から始まった旅のときだけ |
| スクロール位置 | あえて復元しない — 行へのフォーカスの方が正確。1 行挿入されれば同じピクセル位置は別の行を指す |
| 編集途中の入力 | 復元しない — [未保存の変更](/hypermedia-components/ja/recipes/unsaved-changes/)が離脱前に警告 |

詳細側はその URL を知る必要があります。アプリが正準の一覧を知っているか、
リンクが `?from=<URL エンコード>` で運ぶかです。運ぶ場合は**サーバー側で
検証**してから（同一オリジン・既知のルート）リンクに出してください。
未検証の `from` はオープンリダイレクトです。

## 「同じ」より「新しい」

| ケース | 戻り方 |
| --- | --- |
| 何も変更していない | **Back**。ブラウザ自身の履歴が最も安く正しい復元 |
| 保存した | 一覧 URL ＋ `#row-<id>` へ **`303`** |

このリダイレクトは形式ではありません。スナップショット復元は
*ユーザー自身の編集より前*のデータを見せます。確実に気付かれる唯一の
古い値であり、ページャの件数も道連れです。

:::caution[boost したリンクは古さを呼び戻します]
htmx は boost したページを履歴スナップショットから復元します。行リンクは
boost しない（通常遷移なら Back が完璧です）か、一覧に
`hx-history="false"` を付けて復元時に再取得させてください。
:::

## のぞき見かページか：URL は 1 つ、描画は 2 つ

```html
<a href="/orders/4903" data-hc-row-link
   data-hx-get="/orders/4903?peek=1"
   data-hx-target="#record-dialog"
   data-hx-swap="innerHTML">SO-4903</a>
```

`href` は正準のままなので、JavaScript が落ちても「死んだ行」ではなく
フルページになります。オーバーレイは
[remote-dialog](/hypermedia-components/ja/recipes/remote-dialog/)
レシピで、**中にフルページへのリンクを含みます** — 閉じ込めるのぞき見は
無い方がましです。

## 連続して辿る

1 件開くのは仕事の半分です。業務は待ち行列です。詳細は前へ／次へを
持ち、その並びはユーザーが見ていたものです。

| 並び | ワイヤ |
| --- | --- |
| **結果セット**（既定） | `?seq=list&i=<序数>` — サーバーが一覧クエリを再実行して前後を解決するので、「次へ」はページ境界をクライアントに意識させずに越えます |
| **選択** | 「選択した12件を開く」が `ids` チェックボックスを送信 → `POST /orders/selections` → トークン付きで `303` |

トークンが指すのは**順序付きスナップショット**で、画面もそう述べます
（*12 件中 3 件目 — 14:32 時点の選択*）。消えたレコードは**墓標の
ステップ**であって壁ではありません（最初の欠落で止まると、データが
動いているときにこそ信用できなくなります）。トークンの失効は
**fail closed**（`410` ＋一覧への導線）で、黙って全件歩行に広がる
ことはありません。

## 関連

- [データグリッド](/hypermedia-components/ja/components/datagrid/) —
  行の序数、フラグメントナビゲーション、アクティブセル
- [一括操作](/hypermedia-components/ja/recipes/datagrid-bulk-actions/) —
  選択の出どころ
- [リモートダイアログ](/hypermedia-components/ja/recipes/remote-dialog/) —
  のぞき見の描画
- [データグリッドページ](/hypermedia-components/ja/templates/data-grid-page/) —
  これが起きる画面

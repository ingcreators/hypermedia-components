---
title: 設計思想
description: Hypermedia Components の設計原則。
---

1. **標準 HTML ファースト** — ネイティブの `button`・`input`・`dialog`・`popover` が第一の API です。
2. **Light DOM ファースト** — Shadow DOM は使いません。htmx 属性もフォームも普通に動きます。
3. **セマンティックなクラス** — 長いユーティリティクラスの羅列ではなく `hc-button`。
4. **状態は HTML 属性に** — `data-*`、`aria-*`、ネイティブ属性がスタイリングを駆動します。
5. **ネットワークは htmx の責務** — ビヘイビアは `fetch()` をラップしません。
6. **ビヘイビアは小さく** — 確認、トースト、ダイアログを閉じる、フォーカス管理。
7. **DTCG トークンが視覚上の唯一の情報源** — CSS はカスタムプロパティを消費します。
8. **マクロは任意** — すべてのマクロは展開後の HTML をドキュメント化します。

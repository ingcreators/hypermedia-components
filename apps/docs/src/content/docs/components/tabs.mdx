---
title: Tabs
description: Accessible tab panels following the WAI-ARIA APG pattern, with progressive enhancement for browser find-in-page and an optional URL-routed variant.
---

import UsedInRecipes from '../../../components/UsedInRecipes.astro';

import Demo from '../../../components/Demo.astro';

`hc-tabs` ships two markup patterns under the same classnames and visual
style:

| Pattern | When | Markup | Needs JS? |
| --- | --- | --- | --- |
| **App-state** | A single page swaps panels without changing the URL. | `<div role="tablist">` + `<button role="tab">` + `<div role="tabpanel">` | Yes — `installTabs()` wires roving tabindex, arrow keys, and panel toggling. |
| **URL-routed** | Each tab is its own URL or query parameter. | `<nav>` + `<a href>` with `aria-current="page"` | No — the browser's native link semantics already handle keyboard and focus. |

The app-state pattern follows the [WAI-ARIA APG Tabs pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/) and uses
**manual activation by default** so panels can lazy-load over htmx
without firing a request on every arrow key.

*Also known as: tab bar, tabbed interface.*

## App-state tabs

<Demo>
  <div class="hc-tabs">
    <div class="hc-tabs__list" role="tablist" aria-label="Account preview">
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-tab-general" aria-controls="doc-panel-general"
              aria-selected="true" tabindex="0">General</button>
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-tab-billing" aria-controls="doc-panel-billing"
              aria-selected="false" tabindex="-1">Billing</button>
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-tab-team" aria-controls="doc-panel-team"
              aria-selected="false" tabindex="-1">Team</button>
    </div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-panel-general" aria-labelledby="doc-tab-general"
         tabindex="0">
      General account settings.
    </div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-panel-billing" aria-labelledby="doc-tab-billing"
         tabindex="0" hidden="until-found">
      Billing details.
    </div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-panel-team" aria-labelledby="doc-tab-team"
         tabindex="0" hidden="until-found">
      Team members.
    </div>
  </div>

  <Fragment slot="code">

  ```html
  <div class="hc-tabs">
    <div class="hc-tabs__list" role="tablist" aria-label="Account">
      <button type="button" class="hc-tabs__tab" role="tab"
              id="tab-general" aria-controls="panel-general"
              aria-selected="true" tabindex="0">General</button>
      <button type="button" class="hc-tabs__tab" role="tab"
              id="tab-billing" aria-controls="panel-billing"
              aria-selected="false" tabindex="-1">Billing</button>
      <button type="button" class="hc-tabs__tab" role="tab"
              id="tab-team" aria-controls="panel-team"
              aria-selected="false" tabindex="-1">Team</button>
    </div>
  
    <div class="hc-tabs__panel" role="tabpanel"
         id="panel-general" aria-labelledby="tab-general"
         tabindex="0">
      General settings panel.
    </div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="panel-billing" aria-labelledby="tab-billing"
         tabindex="0" hidden="until-found">
      Billing details panel.
    </div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="panel-team" aria-labelledby="tab-team"
         tabindex="0" hidden="until-found">
      Team members panel.
    </div>
  </div>
  ```

  </Fragment>
</Demo>

```js
import { installTabs } from '@hypermedia-components/core';
installTabs();
```

`installTabs()` is idempotent and returns an uninstaller. The zero-config
`@hypermedia-components/core/behaviors` entry auto-installs it and picks
up content swapped in by htmx.

When a panel becomes active, the behavior dispatches a bubbling
**`hc:tabactivated`** event on that panel (no `detail` payload — the
event target *is* the panel). Use it to react to tab switches, e.g. to
lazy-load a panel's content (see [htmx usage](#htmx-usage)).

### Activation mode

By default the behavior uses **manual activation** — arrow keys move
focus, and the user confirms with `Enter` or `Space`. This is the
right default when panels lazy-load (htmx, IntersectionObserver) since
focus alone does not trigger a request.

To activate panels as soon as the tab receives focus, opt in with
`data-activation="automatic"`:

```html
<div class="hc-tabs" data-activation="automatic">…</div>
```

### Variants

<Demo>
  <div class="hc-tabs" data-variant="pill">
    <div class="hc-tabs__list" role="tablist" aria-label="View preview">
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-pill-day" aria-controls="doc-pill-panel-day"
              aria-selected="true" tabindex="0">Day</button>
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-pill-week" aria-controls="doc-pill-panel-week"
              aria-selected="false" tabindex="-1">Week</button>
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-pill-month" aria-controls="doc-pill-panel-month"
              aria-selected="false" tabindex="-1">Month</button>
    </div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-pill-panel-day" aria-labelledby="doc-pill-day"
         tabindex="0">Day view.</div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-pill-panel-week" aria-labelledby="doc-pill-week"
         tabindex="0" hidden="until-found">Week view.</div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-pill-panel-month" aria-labelledby="doc-pill-month"
         tabindex="0" hidden="until-found">Month view.</div>
  </div>

  <Fragment slot="code">

  ```html
  <div class="hc-tabs" data-variant="pill">…</div>
  ```

  </Fragment>
</Demo>

| `data-variant` | Style |
| -------------- | ----- |
| `default`      | Underline indicator on the active tab. |
| `pill`         | Filled background on the active tab; the list drops its baseline. |

### Sizes

`data-size` accepts `sm`, `md` (default), and `lg`. The default size
also follows `data-density`, so dense layouts shrink without per-tab
work.

```html
<div class="hc-tabs" data-size="sm">…</div>
```

### Overflow (scrollable)

By default a tab list **wraps** to a second line when it runs out of room.
Add `data-overflow="scroll"` to keep it on **one horizontally-scrollable row**
instead:

```html
<div class="hc-tabs" data-overflow="scroll">
  <div class="hc-tabs__list" role="tablist" aria-label="Sections">
    <button type="button" class="hc-tabs__tab" role="tab" …>Overview</button>
    <!-- …many tabs… -->
  </div>
  <!-- panels -->
</div>
```

`installTabs()` does the rest:

- It injects **edge scroll buttons** (a mouse affordance) that appear only
  when there is more to scroll in that direction. They sit outside the tab
  order — keyboard users rely on the arrow keys, which already scroll.
- The **active and focused tabs are kept in view**: arrow-key navigation,
  activation, and the initially-selected tab all scroll into the visible row.
- Touch / trackpad scroll works natively; the scrollbar is hidden.
- It is direction-aware — in RTL the buttons flip to the other edge and the
  chevrons mirror.

The scroll buttons are decorative (`aria-hidden`), so this is purely additive
to the accessible tab pattern.

### Vertical orientation

Add `data-orientation="vertical"` to the root to stand the tab list up as a
column beside its panels. `installTabs()` reflects this onto the tablist's
`aria-orientation="vertical"`, which flips the arrow-key axis: <kbd>↑</kbd> /
<kbd>↓</kbd> move between tabs (instead of <kbd>←</kbd> / <kbd>→</kbd>), while
<kbd>Home</kbd> / <kbd>End</kbd> and activation are unchanged. The active
indicator moves from the underline to an inline-start bar (logical, so it
flips in RTL).

<Demo>
  <div class="hc-tabs" data-orientation="vertical">
    <div class="hc-tabs__list" role="tablist" aria-orientation="vertical" aria-label="Workspace preview">
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-vtab-overview" aria-controls="doc-vpanel-overview"
              aria-selected="true" tabindex="0">Overview</button>
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-vtab-activity" aria-controls="doc-vpanel-activity"
              aria-selected="false" tabindex="-1">Activity</button>
      <button type="button" class="hc-tabs__tab" role="tab"
              id="doc-vtab-settings" aria-controls="doc-vpanel-settings"
              aria-selected="false" tabindex="-1">Settings</button>
    </div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-vpanel-overview" aria-labelledby="doc-vtab-overview"
         tabindex="0">Overview panel.</div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-vpanel-activity" aria-labelledby="doc-vtab-activity"
         tabindex="0" hidden="until-found">Activity panel.</div>
    <div class="hc-tabs__panel" role="tabpanel"
         id="doc-vpanel-settings" aria-labelledby="doc-vtab-settings"
         tabindex="0" hidden="until-found">Settings panel.</div>
  </div>

  <Fragment slot="code">

  ```html
  <div class="hc-tabs" data-orientation="vertical">
    <div class="hc-tabs__list" role="tablist" aria-orientation="vertical" aria-label="Workspace">
      <button type="button" class="hc-tabs__tab" role="tab" …>Overview</button>
      <!-- … -->
    </div>
    <!-- panels -->
  </div>
  ```

  </Fragment>
</Demo>

`installTabs()` sets `aria-orientation` for you, but include it in the markup
too so the orientation is correct before JavaScript runs. Vertical and the
scrollable overflow row are separate layouts — don't combine them.

## URL-routed tabs

When each tab is its own route, drop the ARIA tab roles and let the
browser treat the markup as ordinary navigation. `aria-current="page"`
marks the active tab; `installTabs()` ignores this variant.

<Demo>
  <div class="hc-tabs">
    <nav class="hc-tabs__list" aria-label="Documentation preview">
      <a class="hc-tabs__tab" href="#overview" aria-current="page">Overview</a>
      <a class="hc-tabs__tab" href="#api">API</a>
      <a class="hc-tabs__tab" href="#changelog">Changelog</a>
    </nav>
  </div>

  <Fragment slot="code">

  ```html
  <div class="hc-tabs">
    <nav class="hc-tabs__list" aria-label="Documentation">
      <a class="hc-tabs__tab" href="/docs/overview" aria-current="page">Overview</a>
      <a class="hc-tabs__tab" href="/docs/api">API</a>
      <a class="hc-tabs__tab" href="/docs/changelog">Changelog</a>
    </nav>
  </div>
  ```

  </Fragment>
</Demo>

## htmx usage

A panel can fetch its content the first time it becomes active by
listening for `hc:tabactivated` — the event the behavior dispatches
when a panel is revealed.

```html
<div class="hc-tabs__panel" role="tabpanel"
     id="panel-billing" aria-labelledby="tab-billing"
     tabindex="0" hidden="until-found"
     data-hx-get="/account/billing"
     data-hx-trigger="hc:tabactivated once"
     data-hx-target="this"
     data-hx-swap="innerHTML">
  <span class="hc-spinner" aria-hidden="true"></span>
</div>
```

## Find-in-page

Inactive panels carry `hidden="until-found"` so the browser's Ctrl+F
can search them. When the user's query matches text inside a hidden
panel the browser fires `beforematch`; the behavior catches it and
auto-switches to the owning tab so the match is visible. Browsers
without [`hidden="until-found"`](https://developer.mozilla.org/docs/Web/HTML/Global_attributes/hidden)
treat the attribute as a plain `hidden`.

## Accessibility

- The app-state pattern follows the WAI-ARIA APG. Roles: `tablist`,
  `tab`, `tabpanel`.
- Each tab needs `aria-controls` pointing at its panel; each panel
  needs `aria-labelledby` pointing back at its tab.
- The active tab carries `aria-selected="true"` and `tabindex="0"`;
  inactive tabs carry `aria-selected="false"` and `tabindex="-1"`
  (roving tabindex pattern). Disabled tabs use `aria-disabled="true"`
  and are skipped by arrow navigation.
- The tablist must have an accessible name. Use `aria-label` or
  `aria-labelledby` on the `[role="tablist"]` element.
- A vertical tablist carries `aria-orientation="vertical"` (set by
  `installTabs()` from `data-orientation`); the arrow-key axis follows it
  — <kbd>↑</kbd> / <kbd>↓</kbd> when vertical, <kbd>←</kbd> / <kbd>→</kbd>
  when horizontal.
- Panels are programmatically focusable (`tabindex="0"`) so keyboard
  users can `Tab` into their content directly.
- The URL-routed variant inherits all navigation accessibility from
  `<nav>` + `<a>`. Do not add ARIA tab roles to it — that would
  conflict with the navigation pattern.

## Theming tokens

Component tokens (in `component.tokens.json`):

| Token path                            | Purpose                                  |
| ------------------------------------- | ---------------------------------------- |
| `tabs.list.gap` / `list.border`       | Gap between tabs and bottom border.      |
| `tabs.tab.height` / `padding-x`       | Inherits `--hc-control-*` (density).     |
| `tabs.tab.fg` / `hover-fg` / `hover-bg` / `active-fg` | Tab text and hover surface. |
| `tabs.tab.indicator` / `indicator-size` | Active-state underline.                |
| `tabs.tab.disabled-fg`                | Tab text when `aria-disabled="true"`.    |
| `tabs.pill.active-bg` / `active-fg`   | Filled background for `data-variant="pill"`. |
| `tabs.panel.padding-y` / `focus-ring` | Panel breathing room and focus outline.  |
| `tabs.sm.*` / `tabs.lg.*`             | Dedicated sm / lg overrides.             |

## CSS variables

<details>
<summary>Show the generated CSS variables</summary>

```text
--hc-tabs-list-gap | -list-border
--hc-tabs-tab-height | -padding-x | -font-size | -font-weight | -radius
--hc-tabs-tab-fg | -hover-fg | -hover-bg | -active-fg
--hc-tabs-tab-indicator | -indicator-size
--hc-tabs-tab-disabled-fg
--hc-tabs-pill-active-bg | -pill-active-fg
--hc-tabs-panel-padding-y | -panel-focus-ring
--hc-tabs-scroll-size   /* width of the overflow scroll buttons; default 2rem */
--hc-tabs-sm-tab-height | -sm-tab-padding-x | -sm-tab-font-size
--hc-tabs-lg-tab-height | -lg-tab-padding-x | -lg-tab-font-size
--hc-control-height (inherited from data-density)
--hc-color-action-primary-bg | -primary-fg (inherited from data-color)
--hc-color-focus-ring
```

</details>

## Related

- [Dialog](/hypermedia-components/components/dialog/) — modal alternative for grouping content that should not stay visible.
- [Popover](/hypermedia-components/components/popover/) — transient sibling.
- [Toolbar](/hypermedia-components/components/toolbar/) — horizontal action cluster (often paired with tabs).

<UsedInRecipes slug="tabs" />

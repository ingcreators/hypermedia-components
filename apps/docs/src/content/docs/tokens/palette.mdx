---
title: Color palette
description: The primitive color ramps that token references resolve against. These are the raw values behind every semantic and component token — and the shades you reference when authoring a custom theme.
sidebar:
  order: 5
---

import PrimitivePalette from '../../../components/PrimitivePalette.astro';

These are the **primitive** color ramps — the bottom layer of the
[four-layer token model](/hypermedia-components/tokens/). Every
semantic, component, and theme token ultimately resolves to one of the
shades below.

:::note[Not a CSS API]
Primitives are `emit: false` — they are **not** output as `--hc-*`
custom properties, so you can't `var()` them. They exist only as the
values that DTCG `{ref}`s resolve against. Reference them by token path
(`{primitive.color.indigo.600}`) when authoring a theme, **not** as
CSS variables. To consume a color at runtime, use the
[semantic](/hypermedia-components/tokens/) or `data-color` tokens, which
*are* emitted.
:::

Each ramp runs the standard Tailwind 50–950 scale. The five neutral
ramps add a tuned `350` step — the dark-theme `text-muted` shade,
chosen so muted text keeps ≥ 4.5:1 contrast even on `muted-bg`
(the ramp's `700`) surfaces. The `gray` ramp is
the project's default neutral; `slate` / `zinc` / `neutral` / `stone`
are the alternative neutrals offered by the
[Theme builder](/hypermedia-components/tokens/theme-builder/). The
accent / status hues back the built-in `data-color` themes and the
fixed semantic `info` / `success` / `warning` / `error` colors.

<PrimitivePalette />

## Referencing a shade

When you author a custom theme (see
[Authoring your own theme](/hypermedia-components/tokens/themes/#authoring-your-own-theme)),
point a DTCG token at a primitive by its path:

```json
{
  "color": {
    "action": {
      "primary": {
        "bg": { "$type": "color", "$value": "{primitive.color.violet.600}" }
      }
    }
  }
}
```

Hover any swatch above to see its exact `{primitive.color.*}` path and
hex. The build resolves the reference to the concrete value; the
primitive itself is never emitted.

## Related

- [Tokens overview](/hypermedia-components/tokens/) — the four-layer model.
- [Color themes](/hypermedia-components/tokens/themes/) — the `data-color` accent axis built on these ramps.
- [Theme builder](/hypermedia-components/tokens/theme-builder/) — generate a custom theme; the neutral-ramp picker uses the ramps above.
